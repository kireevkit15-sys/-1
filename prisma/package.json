{
  "name": "@razum/prisma",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "ts-node --project tsconfig.json seed.ts",
    "db:reset": "prisma migrate reset",
    "db:seed-demo": "npx tsx seed-demo.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0"
  },
  "devDependencies": {
    "prisma": "^6.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "@types/node": "^22.13.10",
    "bcrypt": "^5.1.1",
    "@types/bcrypt": "^5.0.2"
  },
  "prisma": {
    "seed": "ts-node --project tsconfig.json seed.ts"
  }
}
